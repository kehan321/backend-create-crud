<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <h1>Welcome to <%= title %></h1>
  <h2>User List</h2>
  <ul>
    <% users.forEach(user => { %>
      <li><%= user.name %> (<%= user.email %>, <%= user.age %> years old)</li>
    <% }); %>
  </ul>
  <a href="/add-user">Add User</a>
</body>
</html>
